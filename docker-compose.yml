services:
  app:
    build: .
    image: pavelvais/claude-inspect
    container_name: claude-inspect
    ports:
      - "3000:3000"
    volumes:
      - ${TARGET_PROJECT_PATH:-.}:/inspect:ro
    environment:
      - PROJECT_PATH=/inspect
